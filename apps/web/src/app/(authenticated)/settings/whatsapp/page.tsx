import { WhatsAppGatewayView } from '@/components/page/settings/WhatsAppGatewayView';

export const metadata = {
  title: 'WhatsApp Gateway Engine | ERP Enterprise',
  description: 'Pengaturan & Notifikasi WhatsApp Gateway untuk Ticket & Incident Alert.'
};

export default function Page() {
  return <WhatsAppGatewayView />;
}
